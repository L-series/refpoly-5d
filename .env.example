# Copy to .env and fill in. Used by scripts/download_ws5d.py.
# The ws-5d dataset is public, so the token is optional but recommended to
# avoid HuggingFace rate limits on large downloads.
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
